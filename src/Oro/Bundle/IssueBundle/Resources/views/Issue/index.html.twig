{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set pageTitle = 'issue.issue'|trans %}

{% block navButtons %}
    {% if resource_granted('issue_view') %}
        <div class="btn-group">
            {{ UI.addButton({
                'path': path('issue_create'),
                'entity_label': 'issue.issue'|trans
            }) }}
        </div>
    {% endif %}
{% endblock %}
