{% extends 'OroDashboardBundle:Dashboard:widget.html.twig' %}
{% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

{% block content %}
    {{ dataGrid.renderGrid('widget-short-issue-grid', {'user_id': user.id}) }}
{% endblock %}

{% block actions %}
    {% set actions = [{
    'url': path('issue_index'),
    'type': 'link',
    'label': 'issue.fullist'|trans
    }] %}

    {{ parent() }}
{% endblock %}
